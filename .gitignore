# Codex remote attachments (image artifacts from Codex runs)
.codex-remote-attachments/

# Node / build
node_modules/

# Local visual QA contains user document photos; never commit these images.
backups/*.png
backups/*.patch
