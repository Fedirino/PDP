# Codex remote attachments (image artifacts from Codex runs)
.codex-remote-attachments/

# Node / build
node_modules/

# Local backups (photo-editor artifacts / saved drafts) — not for tracking
backups/