# Netlify configuration for the Produce Department Portal

[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  included_files = []

[functions.scan]
  timeout = 26

[[redirects]]
  from = "/api/scan"
  to = "/.netlify/functions/scan"
  status = 200
  force = true
