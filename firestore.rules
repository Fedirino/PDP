rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /meta/{doc} {
      allow read, write: if request.auth != null;
    }
    match /dailynotes/{doc} {
      allow read, write: if request.auth != null;
    }
    match /salesplan/{doc} {
      allow read, write: if request.auth != null;
    }
  }
}
